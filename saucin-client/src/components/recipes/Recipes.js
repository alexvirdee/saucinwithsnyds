import React from 'react';
import PropTypes from 'prop-types';

const Recipes = props => {
  return <div className="container mx-auto text-4xl">Recipes Page</div>;
};

Recipes.propTypes = {};

export default Recipes;
